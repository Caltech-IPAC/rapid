# Gitignore file for the RAPID repository

# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]

# uv scratch — identical to smdc's tracked rules, so an eventual merge agrees
uv.lock
.venv/

# C build outputs (built in-tree)
c/builds/

# Pipeline scratch / working area
work/

# Docs build output
docs/build/
docs/**/*.inc

# Editor / local config
.vscode/

# Secrets — never commit
*.env

# Personal scratch notes
alerts/scratch/
