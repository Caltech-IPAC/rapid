"""The contract tier: tests against a real PostgreSQL built from the
authoritative migration stream.

See `pipeline/contract/fixture.py` for how a test reaches the database and
`pipeline/contract/README.md` for how the tier is run in CI and on
rapid-admin.
"""
