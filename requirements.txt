numpy>=1.25
astropy>=6.0
# The alerts NED cross-match (providers.AstroqueryNedReader, on by default)
# queries the NED web service through astroquery. The import is deferred to
# query time, so without it every chip logs a warning and emits a null
# nedMatches instead of failing -- but whatever environment runs alert
# production must carry it for the match to actually run.
astroquery>=0.4.7
# rapidpipe.db and rapidpipe.runs import psycopg2 unconditionally
# (connection.py, ids.py's caller in runs/repository.py); psycopg2-binary
# is the wheel-distributed build, matching what the db-migrations and
# unit-tests CI workflows already install by hand.
psycopg2-binary>=2.9
# rapidpipe.science.spatial computes the level-6/level-9 HEALPix indexes
# register writes at registration time. Wheels exist for manylinux and
# macOS arm64, so no compiler is needed in CI or on a developer laptop.
healpy>=1.17
# rapidpipe.products.storage imports boto3 lazily, inside the functions
# that fetch or publish an S3 --inputs/--outputs location, so importing
# rapidpipe or running a stage entirely against local directories never
# requires it and the unit-tests job need not install it. Whatever
# environment runs a stage against S3 locations must carry it.
boto3>=1.34
# The alerts stage writes its Avro object container with fastavro
# (rapidpipe.science.alerts imports it at module scope; nothing else in
# rapidpipe does). The pipeline image's conda environment carries it.
fastavro>=1.9
# The difference stage's PSF-fit catalogs (rapidpipe.science.difference.psfcat)
# import photutils lazily, inside the fitting functions, so importing the
# stage never requires it; whatever environment runs difference with real
# tools must carry it. The pipeline image's conda environment carries it.
photutils>=2.0
# The export stage's HATS source-catalog export (rapidpipe.stages.export)
# imports hats, hats_import and dask lazily, inside the export path, so
# importing the stage never requires them; hats-import pulls hats and dask.
# The pipeline image's conda environment carries all three.
hats-import>=0.5
